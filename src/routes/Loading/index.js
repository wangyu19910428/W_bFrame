import './index.less';

export default class Loading extends Component{
    constructor(props) {
        super(props);
    }
    render () {
        return (
            <div id='loading'>Loading
            </div>
        )
    }
}